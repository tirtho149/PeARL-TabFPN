# Mirror of pyproject.toml [project.dependencies] for `pip install -r`.
# Prefer `bash SETUP_ENV.sh` (handles CUDA wheel selection + smokes) or
# `pip install -e .` (then `pip install -e .[survival]` for TCGA-BRCA).
#
# IMPORTANT: install torch with the correct CUDA wheel BEFORE the rest so pip
# doesn't pull a generic torch that misses CUDA. SETUP_ENV.sh does this for you.

# Core numerics + IO
numpy>=1.24,<2
pandas>=2.0
scipy>=1.11,<1.16
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Imaging / HEST loaders
h5py>=3.9
anndata>=0.9
scanpy>=1.10
pillow>=10.0

# Progress / HTTP / HuggingFace
tqdm>=4.66
requests>=2.31
huggingface-hub>=0.16

# ML stack — torch>=2.5 required by tabpfn 8 (uses torch.nn.attention API)
torch>=2.5
torchvision>=0.16
timm>=1.0
tabpfn>=8.0,<9

# TCGA-BRCA survival arm (Figure/Table 3). Comment out if not running survival.
openslide-bin>=4.0
openslide-python>=1.4
tifffile>=2024.1
lifelines>=0.27
